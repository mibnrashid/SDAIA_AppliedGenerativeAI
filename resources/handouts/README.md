# Handouts

Drop files here (PDF, slides, spreadsheets, anything), then add an entry to
`../resources.json` pointing at `./handouts/your-file.pdf`.

Nothing in this folder is loaded automatically — the JSON is the index.
